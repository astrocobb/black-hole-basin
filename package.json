{
  "name": "black-hole-basin",
  "private": true,
  "workspaces": [
    "frontend",
    "backend/api"
  ],
  "scripts": {
    "dev:backend": "npm run dev --prefix backend/api",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "eslint": "^9.9.0",
    "prettier": "^3.3.3",
    "typescript": "^5.9.3"
  }
}
