services:
  db:
    image: postgis/postgis:16-3.5
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: astro
      POSTGRES_PASSWORD: password
      POSTGRES_DB: black-hole-basin-db
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  pgdata: